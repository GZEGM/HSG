#include <iostream>

using namespace std;

int main()
{

    int n, k;
    int arr[n];

    freopen("DelElement.inp", "r", stdin);
    freopen("DelElement.out", "w", stdout);

    cin >> n >> k;

    int res = n;

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    for (int i = 2;)

        return 0;
}
