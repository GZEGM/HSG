// Bài 1. Tìm giữa(6 điểm)
// Cho hai số nguyên dương 𝐿 và 𝑅. Yêu cầu : Tìm số nguyên dương 𝑀(𝐿≤𝑀 < 𝑅) để chênh lệch giữa tổng các số nguyên liên tiếp từ 𝐿
// đến 𝑀 và tổng các số nguyên liên tiếp từ 𝑀 + 1 đến 𝑅 là nhỏ nhất.

//         Dữ liệu vào từ tệp BAI1.INP : Gồm hai số nguyên dương 𝐿 và 𝑅(𝐿 < 𝑅≤10).

//         Kết quả ra ghi vào tệp BAI1.OUT : Gồm một số nguyên duy nhất là số 𝑀 thoả mãn

// • Có 60 % số test : 𝐿 < 𝑅≤10;
// • Có 40 % số test còn lại : 𝐿 < 𝑅≤10.

#include <iostream>

using namespace std;

int main()
{
    int L, R;
    freopen("BAI1.INP", "r", stdin);
    freopen("BAI1.OUT", "w", stdout);
    cin >> L >> R;

    return 0;
}
