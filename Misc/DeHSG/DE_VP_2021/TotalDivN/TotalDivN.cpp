#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int m, n;
    int res = 0;

    int arr[m];

    freopen("TotalDivN.inp", "r", stdin);
    freopen("TotalDivN.out", "w", stdout);

    cin >> m >> n;

    for (int i = 0; i < m; i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < m; j++)
        {
        }
    }

    return 0;
}
